sudo: required
services: 
  - docker

before_install:
  - docker build -t rohitaror/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rohitaror/docker-react npm run test -- --coverage